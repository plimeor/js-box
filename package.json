{
  "private": true,
  "scripts": {
    "build": "pnpm --filter './packages/*' build",
    "prepublish": "pnpm build",
    "publish": "pnpm publish --filter './packages/*' --access public --publish-branch main --report-summary"
  },
  "devDependencies": {
    "typescript": "next"
  }
}
